paths:
  uploads: public/images/portfolio
  static: public
collections_config:
  productos:
    path: public/productos
    output: false
    icon: shopping_cart
    allow_create: true # ESTA LÍNEA ES VITAL
  
  portafolio:
    path: public/portafolio
    output: false
    icon: collections
    allow_create: true # AGREGA ESTO AQUÍ TAMBIÉN
    fields:
      title:
        type: string
      imageUrl:
        type: image
